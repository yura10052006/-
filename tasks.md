# tasks.md — Трек A: задачі з критеріями готовності

> **Статус:** чернетка на затвердження. Код починаю писати ТІЛЬКИ після твого «ок».
> **Базується на:** `spec.md` + `plan.md`.
> **Формат кожної задачі:** що робимо → критерій готовності (DoD — Definition of Done) → хто робить.
> **«Хто»:** 🤖 = я (Claude Code) роблю сам; 👤 = потрібна твоя дія (ключ, телефон, перевірка).
> **Дата:** 2026-07-24

Порядок за принципом *fail fast* — найризикованіше й найдешевше для перевірки — раніше.
Бекенд можна повністю тестувати **без телефона** (curl), тому він іде першим після механіки.

---

## M0 — Санітарні перевірки та підготовка (роблять паралельно з M1)

### T-0.1 👤 Завести ключі та акаунти
- Anthropic API key (console.anthropic.com).
- OpenAI API key (для Whisper).
- Todoist API token (Todoist → Settings → Integrations → Developer → API token).
- **DoD:** три ключі є у тебе під рукою (покладеш у `.env` бекенда, коли скажу; в git вони не потраплять).

### T-0.2 👤 Перевірити український голос у системному TTS на Redmi 13 Pro
- Налаштування → Мова → Синтез мовлення / Text-to-speech → чи є українська; за потреби — доставити голосовий пакет.
- **DoD:** знаємо «так/ні» щодо укр-голосу. Якщо «ні» — вмикаю резервний план TTS (позначено в plan §4.5).

### T-0.3 👤 Підтвердити наявність Bluetooth-навушників з мікрофоном
- **DoD:** знаємо, чи є BT-гарнітура зараз. Якщо ні — M4 стартує на мікрофоні телефона (8 кГц), M5 (HFP) відкладаємо до появи заліза/окулярів.

### T-0.4 🤖 Перенести старий каркас у `legacy/`
- Перемістити весь поточний Android-проєкт «Калькулятор зарплати» (`app/`, `gradle/`, `build.gradle.kts`, `settings.gradle.kts`, `gradle.properties`, `metadata.json`, `assets/`) → `legacy/salary-calculator/`.
- Оновити кореневий `README.md` під нову структуру (два компоненти).
- **DoD:** корінь репозиторію чистий (лишились лише `spec/plan/tasks.md`, `README.md`, `legacy/`); старий код фізично збережено в `legacy/`, git-історія ціла.

---

## M1 — Скелет бекенда (backend skeleton)

### T-1.1 🤖 Створити структуру `backend/` і залежності
- `backend/app/` за структурою plan §3.2, `requirements.txt`, `.env.example`, `.gitignore`, `README.md`.
- **DoD:** `pip install -r requirements.txt` проходить; структура тек на місці.

### T-1.2 🤖 FastAPI-скелет: `/health` і `/command` (заглушка)
- `/health` → `{"status":"ok"}`.
- `/command` приймає multipart `audio` + `X-App-Token`, поки що повертає фейкові `recognized_text`/`reply_text`.
- Перевірка `X-App-Token` (403 при невірному).
- **DoD:** локально `uvicorn` піднімається; `curl` на `/health` і `/command` (з тестовим WAV) віддає очікуваний JSON; невірний токен → 403.

---

## M2 — STT (Whisper)

### T-2.1 🤖 Модуль `stt.py` з абстракцією `Transcriber`
- Інтерфейс `Transcriber.transcribe(bytes) -> str`; реалізація `WhisperTranscriber` (`whisper-1`, `language="uk"`).
- Підключити в `/command` замість заглушки STT.
- **DoD:** `curl` з реальним WAV укр-мовлення повертає коректний `recognized_text`.

### T-2.2 👤🤖 Тест STT саме на 8 кГц (NFR-2)
- Ти надиктовуєш кілька фраз-задач, я проганяю через ендпоінт WAV у 8 кГц моно.
- **DoD:** прості команди («додай задачу купити каву», «нагадай завтра подзвонити мамі») розпізнаються стабільно. Якщо систематично псує — фіксуємо як ризик R-5 (сигнал щодо окулярів).

---

## M3 — Агент + Todoist (серце: tool use)

### T-3.1 🤖 Інструмент `tools/todoist.py`
- `create_task(content, due_string?)` → Todoist REST v2; повертає `{id, content, url, ok}`.
- **DoD:** прямий виклик функції реально створює задачу — видно в застосунку Todoist.

### T-3.2 🤖 Цикл tool use `agent.py`
- System-промпт (короткі укр-відповіді, виклик `create_task` за потреби), tool schema, цикл `messages.create` → `tool_use` → `tool_result` → фінал. Модель Haiku 4.5.
- **DoD:** передаю в агент текст «додай задачу купити каву» → задача створюється в Todoist, `reply_text` = коротке укр-підтвердження.

### T-3.3 🤖 Склеїти повний бекенд-конвеєр у `/command`
- `audio → STT → agent → JSON` з `action` і `timings_ms`; обробка помилок (plan §6) — усі гілки повертають озвучуваний `reply_text`.
- **DoD:** `curl` з WAV «додай задачу купити каву» → задача в Todoist + JSON з `reply_text` і таймінгами. **Це вже 80% Definition of Done, без телефона.**

### T-3.4 🤖👤 Деплой бекенда (або ngrok)
- Локально + `ngrok` для розробки; за бажанням деплой на Railway/Render.
- **DoD:** є HTTPS-URL, доступний з телефона; `/health` відповідає ззовні.

---

## M4 — Android-клієнт (тонкий)

### T-4.1 🤖 Новий Android-проєкт `android/` (Compose)
- Каркас, `minSdk 26`, дозволи в маніфесті (`RECORD_AUDIO`, `BLUETOOTH_CONNECT`, `INTERNET`, `MODIFY_AUDIO_SETTINGS`), `Config.kt` (BASE_URL, токен).
- **DoD:** проєкт збирається в Android Studio, ставиться на Redmi, показує порожній екран.

### T-4.2 🤖 Запис аудіо `AudioRecorder` (8 кГц моно WAV) + дозволи
- `AudioRecord` 8000 Гц/моно/16-bit → WAV-байти; runtime-запит дозволів.
- **DoD:** утримання кнопки пише, відпускання дає WAV; можна зберегти/прослухати для перевірки.

### T-4.3 🤖 Мережа `BackendClient` (OkHttp multipart)
- `POST /command` з `X-App-Token`, парсинг JSON-відповіді.
- **DoD:** з телефона надсилаю записаний WAV → отримую `recognized_text`/`reply_text` від живого бекенда.

### T-4.4 🤖 TTS `Tts.kt` + озвучування відповіді
- `TextToSpeech` uk-UA; озвучити `reply_text`. (Гілка резерву, якщо укр-голосу нема — з T-0.2.)
- **DoD:** відповідь бекенда звучить у навушник/динамік українською.

### T-4.5 🤖 Екран + стейт-машина (`VoiceScreen`/`VoiceViewModel`)
- Кнопка push-to-talk (hold-to-talk), стани IDLE/RECORDING/UPLOADING/SPEAKING/ERROR з підписами, показ `recognized_text` (FR-11), озвучення помилок (FR-10).
- **DoD:** повний цикл з мікрофона **телефона**: кнопка → голос → задача в Todoist → підтвердження у вухо. Стани видно.

---

## M5 — HFP-інтеграція (справжній Bluetooth-мікрофон)
> Лише якщо є BT-навушники (T-0.3). Це найближче до сценарію окулярів.

### T-5.1 🤖 `BluetoothScoManager` — підняти/опустити SCO
- Android 12+ `setCommunicationDevice(TYPE_BLUETOOTH_SCO)`; запис із `VOICE_COMMUNICATION`.
- **DoD:** запис іде саме з мікрофона гарнітури (перевірка: прикрити мікрофон телефона — звук усе одно є).

### T-5.2 🤖 Послідовність SCO(запис) → A2DP(TTS)
- Опустити SCO перед TTS, дочекатись перемикання на A2DP, озвучити.
- **DoD:** запис через HFP, відповідь звучить якісно через A2DP; без зависань. Резерв (R-6): TTS через SCO, якщо перемикання глючить.

---

## M6 — Наскрізний тест і Definition of Done треку A

### T-6.1 👤🤖 Повний E2E на телефоні + навушниках
- Прогнати сценарій із spec §2: кнопка → «додай задачу купити каву» українською → задача в Todoist → підтвердження у вухо.
- **DoD (= Definition of Done треку A):** усі 5 пунктів spec §2 виконуються на реальному пристрої.

### T-6.2 👤 Тиждень «носіння» + метрики
- Користуєшся щодня; нотуєш латентність, % влучань STT, кількість використань, суб'єктивне «чи хочу окуляри 1–5» (spec §8).
- **DoD:** є твоє рішення — гіпотеза підтверджена/спростована → купувати окуляри чи ні (переходити до треків B/C чи ні).

---

## Залежності (dependency graph, стисло)
```
T-0.4(legacy) ─┐
T-0.1(keys) ───┼─► M1 ─► M2 ─► M3 ─► M3.4(deploy) ─► M4 ─► M5* ─► M6
T-0.2(tts)  ───┘                                        (*M5 лише з BT)
T-0.3(bt) ──────────────────────────────────────────────┘
```

## Порядок, який пропоную виконувати
1. **T-0.4** (перенести legacy) — механіка, роблю одразу, ні від чого не залежить.
2. **T-0.1/0.2/0.3** — твої підготовчі (паралельно; T-0.1 потрібен до M2/M3).
3. **M1 → M2 → M3** — бекенд, повністю тестується без телефона (найбільша цінність найраніше).
4. **M4** — Android-клієнт (спершу мікрофон телефона).
5. **M5** — HFP (коли є навушники/окуляри).
6. **M6** — фінальна перевірка гіпотези.

---

## Що далі
Скажи **«ок, tasks затверджено»** — і я почну кодити з **T-0.4** (перенесення legacy) та **M1** (скелет бекенда), бо вони не залежать від твоїх ключів/заліза. Паралельно ти зробиш T-0.1/0.2/0.3.
Якщо хочеш інший порядок чи розбивку — скажи, поправлю перед стартом коду.
