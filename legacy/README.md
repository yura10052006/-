# legacy/

Тут лежить **непов'язаний** зі стрічковим асистентом код, збережений за рішенням **D-5** (`../spec.md`).

## salary-calculator/
Android-каркас «Командний калькулятор зарплати» (згенерований AI Studio, Gemini API).
Потрапив у репозиторій як стартовий шаблон і до голосового AI-асистента (трек A) відношення не має.

**Не видалено навмисно** — зберігаємо історію і код на випадок, якщо щось звідти знадобиться.
Активна розробка треку A відбувається в `../backend/` і `../android/`.
